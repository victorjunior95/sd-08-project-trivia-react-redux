// 6 - lógica do jogo - só uma resposta correta

// 7 - css + logica
// componente Answer + css

// 8 - timer

// 9 - placar

// 10 - botao de proxima pergunta - ele fica hidden
{/* <button
  className="NextStep"
  data-testid="btn-next"
  disabled={
      clickedAnswer && Object.keys(questions).length >= step
      ? false : true
  }
  onClick={() => this.nextStep(step)}
>
  Próxima
</button> */}

