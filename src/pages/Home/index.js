import React from 'react';
import FormLogin from '../../components/FormLogin';

class Home extends React.Component {
  render() {
    return (
      <main>
        <FormLogin />
      </main>
    );
  }
}

export default Home;
