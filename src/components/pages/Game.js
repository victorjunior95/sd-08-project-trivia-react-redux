import React, { Component } from 'react';
import GameHeader from './GameHeader';

class Game extends Component {
  render() {
    return (
      <main>
        <GameHeader />
      </main>
    );
  }
}

export default Game;
